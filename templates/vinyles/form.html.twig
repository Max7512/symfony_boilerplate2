{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-2xl font-bold mb-4">Vinyle</h1>

    {{ form_start(form, {
        'attr': {'class': 'w-full max-w-lg mx-auto'}
    }) }}
        <div class="flex flex-col -mx-3 mb-6">
    {{ form_rest(form) }}
        </div>
    <button type="submit">Enregistrer</button>
    {{ form_end(form) }}
{% endblock %}
